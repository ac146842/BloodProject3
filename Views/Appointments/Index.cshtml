@model IEnumerable<BloodProject3.Models.Appointment>

@{
    ViewData["Title"] = "Appointments";
}

<h1 class="table-header">Appointments</h1>

<p class="btn-create">
    <a asp-action="Create" class="btn btn-dark">Create</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Appointment Time</th>
            <th>@Html.DisplayNameFor(model => model.Location)</th>
            <th>Donor Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.AppointmentDateTime.ToString("hh:mm tt")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @if (item.Donor != null)
                    {
                        @Html.DisplayFor(modelItem => item.Donor.FullName)
                    }
                    else
                    {
                        <span class="text-muted">No Donor Assigned</span>
                    }
                </td>
                <td class="text-end">
                    <a asp-action="Edit" asp-route-id="@item.AppointmentID" class="btn btn-dark">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.AppointmentID" class="btn btn-dark">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.AppointmentID" class="btn btn-dark">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>