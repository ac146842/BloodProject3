@model IEnumerable<BloodProject3.Models.Questions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FormQuestions)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FormQuestions)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HealthQID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HealthQID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HealthQID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
